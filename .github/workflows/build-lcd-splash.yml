name: build-lcd-splash

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/lcd-splash/**'
  pull_request:
    paths:
      - 'debpkgs/lcd-splash/**'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: lcd-splash
    secrets: inherit
