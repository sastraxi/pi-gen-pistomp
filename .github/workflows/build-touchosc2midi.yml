name: build-touchosc2midi

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/touchosc2midi/**'
  pull_request:
    paths:
      - 'debpkgs/touchosc2midi/**'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: touchosc2midi
    secrets: inherit
