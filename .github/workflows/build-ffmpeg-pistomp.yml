name: build-ffmpeg-pistomp

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/ffmpeg-pistomp/**'
  pull_request:
    paths:
      - 'debpkgs/ffmpeg-pistomp/**'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: ffmpeg-pistomp
    secrets: inherit
