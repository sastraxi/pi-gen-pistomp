name: build-jack-capture

on:
  push:
    branches: [main]
    paths:
      - 'debpkgs/jack-capture/**'
  pull_request:
    paths:
      - 'debpkgs/jack-capture/**'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-deb.yml
    with:
      pkg: jack-capture
    secrets: inherit
