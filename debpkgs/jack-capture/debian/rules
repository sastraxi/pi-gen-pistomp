#!/usr/bin/make -f
%:
	dh $@ --with quilt

override_dh_auto_build:
	$(MAKE)

override_dh_auto_install:
	$(MAKE) PREFIX=/usr DESTDIR=$(CURDIR)/debian/jack-capture install
	install -Dm755 jack_capture_gui $(CURDIR)/debian/jack-capture/usr/bin/jack_capture_gui
